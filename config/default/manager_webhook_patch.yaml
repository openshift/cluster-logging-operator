apiVersion: apps/v1
kind: Deployment
metadata:
  name: cluster-logging-operator
spec:
  template:
    spec:
      containers:
      - name: cluster-logging-operator
        ports:
        - containerPort: 9443
          name: webhook-server
          protocol: TCP